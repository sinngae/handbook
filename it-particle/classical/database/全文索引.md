全文索引
---

## sql
```sql
-- 创建
create fulltext index idx_ft_demo on tbl; 

-- 使用
select * from user where match(name) against('aaa');


-- 查看配置
show variables like '%ft%';

```

+ 默认分词间隔符" +-><()~*:\"&|"
+ 默认word最小最大长度 4~84，这个长度限制的才有效
+ 默认token最小最大长度 3~84
+ 。。。