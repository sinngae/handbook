句柄，就是windows操作系统颁发给应用程序的、用于代表和操作这些内核对象的“安全引用”。
句柄本质上是一个非负整数，是 Linux 内核为已打开的资源（如文件、套接字、管道等）所分配的唯一标识符。 在用户层面，程序通过句柄来引用和操作这些资源，而无需关心资源在系统中的具体存储位置和实现细节。 可以将句柄理解为资源的 “身份证号”，系统凭借这个号码精准定位并管理资源。 在 Linux 系统中，最常见的句柄类型是文件描述符（File Descriptor，简称 FD）。